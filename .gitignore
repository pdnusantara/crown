# Backend
backend/node_modules
backend/.env
backend/dist

# Frontend
.env
.env.local
.env.production
node_modules
dist

# Env backups / dumps — JANGAN pernah di-commit (berisi secret)
*.env.bak*
.env.bak*
.env.*.bak
*.env.save

# Docker
*.log
node_modules

# Deploy artifacts (atomic-swap backups)
.deploy/

# Runtime / persisted server state (do not commit)
backend/.wwebjs_cache/
backend/storage/

# Backup DB (data sensitif — jangan commit)
.backups/
