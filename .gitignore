# Backend
backend/node_modules
backend/.env
backend/dist

# Frontend
.env
.env.local
.env.production
node_modules
dist

# Docker
*.log
node_modules

# Deploy artifacts (atomic-swap backups)
.deploy/

# Runtime / persisted server state (do not commit)
backend/.wwebjs_cache/
backend/storage/
